我想让ai帮我用ffmpeg完成这样一个这个功能，并且要求先输出文档，文档里面有详细的架构，以及这样设计的原理，并且一个初学者也能看懂原理。之后再根据文档的设计完成代码的实现。请帮我生成一个提示词:功能具体要求如下：



你现在是我的“资深音视频工程师 + 教学型架构师 + 代码实现助手”。
请严格按“先文档评审通过，再开始编码”的流程工作。

# 项目目标
实现一个“全能格式转换器（Transcoder）”：
- 输入：任意常见封装格式（如 MKV）
- 输出：MP4
- 关键要求：
  1) 流选择：只提取视频流和音频流，忽略字幕流
  2) 编码参数控制：清楚解释并实现 CRF、Preset 对画质/速度/体积的影响
  3) 音视频同步：正确处理 PTS/DTS，保证输出音画同步
- 验证标准：输出文件在主流播放器可正常播放、音画同步、码率符合预期

# 你的工作流程（必须遵守）
## 阶段1：先输出设计文档（暂时不要写代码）
请先输出完整设计文档，面向初学者可读，要求“讲清楚原理 + 可落地实现”。
文档结构必须包含：

1. 需求拆解
   - 功能需求、非功能需求、边界条件（仅音频、仅视频、多音轨、损坏文件等）
2. 总体架构设计
   - 模块划分（参数解析、输入探测、流选择、解复用、解码/重编码、复用、日志与错误处理）
   - 数据流/时序流转（建议用 Mermaid 文本图）
3. 核心原理讲解（初学者友好）
   - 容器 vs 编码格式
   - 流（Stream）、Packet、Frame 的关系
   - 时间基（time_base）、PTS、DTS、duration 的含义与转换
   - 为什么会出现音画不同步，常见成因与修复策略
4. 转码策略设计
   - 视频：CRF + Preset 组合策略与推荐默认值
   - 音频：编码器选择、码率策略
   - 封装：MP4 兼容性注意事项（如 faststart）
5. 流选择策略
   - 如何只保留一条视频流和一条音频流（可配置）
   - 为什么忽略字幕流，后续可扩展点
6. 同步与时间戳设计
   - 统一时钟/主时钟思路
   - 时间戳重映射与 rescale 原理
   - 丢帧/补帧/重采样的决策规则（如需要）
7. 错误处理与健壮性
   - 失败重试、容错、日志分级、返回码规范
8. 性能与质量权衡
   - 速度、画质、体积三者取舍
   - 典型参数模板（高质量、平衡、高速）
9. 测试与验收方案
   - 功能测试、同步测试、兼容性测试
   - 使用 ffprobe 的自动化验收指标
10. 实施计划
   - 分阶段里程碑（MVP -> 增强）
   - 每阶段交付清单

文档要求：
- 语言通俗，尽量“先结论后解释”
- 每个专业术语都要给一句白话解释
- 给出“常见坑 + 规避方式”
- 给出关键 FFmpeg 命令示例（并解释每个参数）

输出完阶段1后，先停下并等待我回复“进入阶段2”。

## 阶段2：根据文档实现代码（收到我确认后再开始）
实现要求：
1. 先给出项目目录结构
2. 分模块逐步实现（每一步先说明目的，再给代码）
3. 代码需包含必要注释，尤其是时间戳处理
4. 提供可执行命令与示例输入输出
5. 提供 README：构建、运行、参数说明、故障排查
6. 提供最小可验证测试脚本（含 ffprobe 校验）
7. 若存在多种实现路线，优先选择“初学者更容易理解和维护”的方案，并说明为何这么选

# 输出风格
- 使用 Markdown
- 标题层级清晰
- 先给“结论摘要”，再给细节
- 任何地方不要跳步：每个关键决策都说明“为什么”

# 约束
- 不要省略 PTS/DTS/time_base 的解释与处理细节
- 不要直接贴一大段代码而无讲解
- 若信息不足，先列出“最少必要澄清问题”（不超过5个）