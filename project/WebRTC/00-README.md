# WebRTC 学习项目 · 索引

> 这是一份面向 **C++ 音视频 / RTC 工程师面试**的 WebRTC 学习项目。
> 文件按学习顺序编号（00→99），**直接按编号顺序读即可**。
> 卡住时 → 查 `99-项目进度存档.md` 看上次到哪儿。

---

## 怎么开始

**第一次来**：直接读 `01-入门导读.md`（60-90 分钟）。建立全景认知后，再按编号顺序往下走。

**回来续上**：先读 `99-项目进度存档.md`（一分钟），知道上次停在哪、下一步做什么。

---

## 文件清单（按编号顺序）

| 编号 | 文件 | 阶段 | 内容 |
|------|------|------|------|
| 00 | `00-README.md` | 入口 | 本文档,索引 |
| 01 | `01-入门导读.md` | **全景** | 6 个核心问题 / 系统全景 / 行业生态 / 3 个月学习路径 / 当前阶段定位 |
| 02 | `02-架构总览.md` | 架构 | 分层 / 三线程模型 / 数据流 / 关键对象生命周期 |
| 03 | `03-音视频基础self-check.md` | 基础 | 40 题自检（YUV/H264/AAC/PTS/FFmpeg/...）|
| 04 | `04-项目设计方案.md` | 设计 | A/B 分层 / 8 模块 DAG / 5 个接口契约 / MVP→V1→V2 路线 |
| 05 | `05-环境准备清单-macOS.md` | 准备 | libwebrtc 获取 / 信令依赖 / coturn / CMake 骨架 |
| 06 | `06-M4-RTP传输模块.md` + `code/` | **B 层模块 1** | RFC 3550/6184 + FU-A 分片 + 13 个 libwebrtc 源码笔记 + 完整 CMake 代码（30 测试） |
| 07 | `07-M6-JitterBuffer模块.md` + `code/` | **B 层模块 2** | 抖动估计 + EWMA + 环形 buffer + 12 个 libwebrtc 源码笔记 + 完整 CMake 代码（27 测试） |
| 08 | `08-SDP真实样本解析.md` | **控制面实战** | 浏览器真实 SDP 逐字段解析 + 面试金句 + 5 道追问 |
| 09 | `09-面试讲述.md` | 面试包装 | 简历项目段 + 3 分钟自述 + 15 分钟深挖 + 10 道高频题 + 白板预案 |
| 99 | `99-学习提示词.md` | 元数据 | 5 阶段执行纲领（A/B 分层硬约束 + 通用规则）|
| 99 | `99-项目进度存档.md` | 元数据 | **回来时先看这个**——当前进度 / 下一步指引 |

---

## 推荐学习路径

```
01 入门导读（建立全景认知，60-90 min）
   ↓
02 架构总览（深入分层）
   ↓
03 音视频基础（self-check 测试，N 项多的回头补）
   ↓
04 项目设计方案（看 A/B 分层和模块依赖图）
   ↓
05 环境准备清单（边搭环境边读）
   ↓
06 M4 RTP 模块（精读 + 跑通 30 测试）
   ↓
07 M6 JitterBuffer（精读 + 跑通 27 测试）
   ↓
08 SDP 真实样本解析（控制面实战，对照真实 SDP 学）
   ↓
09 面试讲述（包装项目，可对外讲）
```

---

## 已完成的成果

| 类别 | 数量 | 备注 |
|------|------|------|
| 主文档 | 10 份 | 约 4400 行 markdown |
| B 层代码 | 2 个独立 CMake 项目 | M4 (~1362 行) + M6 (~1300 行) |
| 单元测试 | 57 个 | 100% 通过 |
| libwebrtc 源码笔记 | 25 个函数 | 含 modules/ 路径 + 行号 + 设计点 |

**当前阶段**：覆盖 WebRTC 约 25%（数据面 RTP + JitterBuffer 两个核心模块 + 控制面 SDP 解析入门）。

---

## 跑 B 层代码（M4 / M6）

```bash
# M4 RTP 模块
cd 06-M4-RTP传输模块/code
mkdir -p build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Debug
cmake --build . -j && ctest --output-on-failure
# 期望: 30 tests passed

# M6 JitterBuffer 模块
cd 07-M6-JitterBuffer模块/code
mkdir -p build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Debug
cmake --build . -j && ctest --output-on-failure
# 期望: 27 tests passed
```

如果之前 build 过现在跑失败 → 删 build 目录重来一次（重命名后路径变了，CMakeCache 里有旧绝对路径）。

---

## 接下来可以深入的方向

读完所有文档后，想继续深入选这几个：

1. **拥塞控制（GCC / BBR）** —— 面试重灾区，行业核心算法
2. **ICE 流程深入** —— NAT 穿透完整实现
3. **写 Adapter 接入 libwebrtc** —— 把 B 层模块注入真实通话链路
4. **音频处理（3A / NetEQ）** —— 音频方向专项

详情见 `01-入门导读.md` 第 13 节。
