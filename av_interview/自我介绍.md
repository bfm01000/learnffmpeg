你好，我叫杜孟林。我21年从西安科技大学毕业后，就直接加入了影石（Insta360），一直做移动端 C++ 开发，到现在有5年了。
在公司这几年，我主要做的是SDK的跨平台开发，也就是用 C++ 写核心的视频处理 Pipeline 和算法落地，然后通过 JNI 和 OC 封装给安卓和 iOS 双端用。
今天面试，我主要想跟您聊聊我做过的三个比较有技术深度的项目：

* 第一个是**自动剪辑的性能优化**。当时因为算法要求高频抽帧，导致解码器疯狂重复解码，特别慢。我把底层的 Seek 策略给改了，换成了“逐帧解码加按需丢帧”，并且用生产者消费者模型把抽帧、渲染和算法推理这三步彻底并行起来，最后出片速度提升了好几倍。

* 第二个是 **Android 4K 全景直播的重构**。当时客户反馈 4K 直播发热严重、疯狂掉帧。我排查发现主要是内存拷贝和网络阻塞的问题。所以我在底层用 `AHardwareBuffer` 打通了零拷贝渲染，又手写了一个基于队列水位的动态码率（ABR）和异步发送机制，最后把弱网下的帧率稳在了 30 帧。优化之前只有22fps左右。

* 第三个是**车载相机预览的低延时优化**。当时有个设备延时高达 1.5 秒，我抓 Trace 发现是渲染前的等待策略把首帧的高延时给“固化”了。为了兼顾低延时和画面平滑，我重新设计了一个基于 AJB（自适应抖动缓冲）的调度器，把端到端延时硬生生压到了 0.5 秒左右。

总的来说，从底层架构到上层业务都能 cover 的开发，对音视频底层和性能调优也一直很有热情。以上就是我的简单介绍，您看我们从哪个项目开始深入聊聊？


下面是我主要想讲的几个项目
* 自动剪辑的算法接入以及性能优化
  * 分析速度优化:
    * GPU，CPU，VPU的并行方案
    * resize放在渲染部分，放在cpu上可能存在重采样，导致运算很多次
  * 防抖数据优化：测试在测试的时候发现长视频在加载的时候，内存占用过大。我们排查发现是一次性加载所有防抖数据导致的，后面经过优化，动态的加载防抖数据，每次只加载60s的防抖数据。当然因为防抖是依赖前后的数据的，因此需要提前加载5s的数据，相当于-5s到55s这个时间。
  * 
  * 时间传递逻辑优化
    * 将帧跟时间戳绑定的逻辑转成跟帧ID强行绑定。
  * 

---

## 🌟 推荐的面试自我介绍（更加口语化、接地气版本）

面试官好，我叫杜孟林。我21年从西安科技大学毕业后，就直接加入了影石（Insta360），一直做移动端 C++ 开发，到现在有5年了。

在公司这几年，我主要做的是音SDK的跨平台开发，也就是用 C++ 写核心的视频处理 Pipeline 和算法落地，然后通过 JNI 和 OC 封装给安卓和 iOS 双端用。最近一年我也在带头做行业端 KMP 架构的迁移。

我平时比较喜欢钻研底层，尤其是抠性能瓶颈。今天面试，我主要想跟您聊聊我做过的三个比较有技术深度的项目：

第一个是**自动剪辑的性能优化**。当时因为算法要求高频抽帧，导致解码器疯狂重复解码，特别慢。我把底层的 Seek 策略给改了，换成了“逐帧解码加按需丢帧”，并且用生产者消费者模型把抽帧、渲染和算法推理这三步彻底并行起来，最后出片速度提升了好几倍。

第二个是 **Android 4K 全景直播的重构**。当时客户反馈 4K 直播发热严重、疯狂掉帧。我排查发现主要是内存拷贝和网络阻塞的锅。所以我在底层用 `AHardwareBuffer` 打通了零拷贝渲染，又手写了一个基于队列水位的动态码率（ABR）和异步发送机制，最后硬是把弱网下的帧率稳在了 30 帧。

第三个是**车载相机预览的低延时优化**。当时有个设备延时高达 1.5 秒，我抓 Trace 发现是渲染前的等待策略把首帧的高延时给“固化”了。为了兼顾低延时和画面平滑，我重新设计了一个基于 AJB（自适应抖动缓冲）的调度器，把端到端延时硬生生压到了 0.5 秒左右。

总的来说，我属于那种能独立扛事儿、从底层架构到上层业务都能 cover 的开发，对音视频底层和性能调优也一直很有热情。以上就是我的简单介绍，您看我们从哪个项目开始深入聊聊？